#ifndef _SETTINGS_H_
#define _SETTINGS_H_

namespace Settings
{
static const bool VSYNC_ENABLED = true; // set the vsync to true ( this should always be true)
static const float FARCLIPPING = 400.0f; // the far clipping plane 
static const float NEARCLIPPING = 1.0f; // the near clipping plane

};

#endif
